#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
#define rep(i, n) for(int i = 0; i < n; i++)

const int lim = 1000000007;

int main(){
    char val;
    cin >> val;
    if(97 <= val && val <= 122) cout << "a" << endl;
    else cout << "A" << endl;
    return 0;
}